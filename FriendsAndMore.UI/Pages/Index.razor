@page "/"

@inherits IndexBase

@if (Contacts == null)
{
    <div class="column">
        <div class="box">
            <h1 class="title">Loading contacts...</h1>
            <progress class="progress is-large is-info" max="100">60%</progress>
        </div>
    </div>
    <div class="spinner"/>
}
else
{
    foreach (var contact in Contacts)
    {
        <ContactTeaser Contact="@contact" />
    }
}